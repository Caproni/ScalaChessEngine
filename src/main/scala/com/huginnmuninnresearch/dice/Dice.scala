package com.huginnmuninnresearch.dice

import scala.util.Random

class Dice(val history: Array[Int] = Array[Int]()) {

//  val rng = new Random()
//
//  def roll(implicit history: Array[Int]): Int = {
//    import Dice._
//
//    val N: Int = history.length
//    val mod: Array[Double] = Array.ofDim(expectations.length)
//
//    val d = rng.nextDouble()
//    val result: Int = for (e <- 0 until expectations.length-1) {
//      d > (expectations(e)+mod(e)) && d < (expectations(e+1)+mod(e+1)) match {
//        case true => e+1
//        case _ => 0
//      }
//    }
//    result
//  }

}

object Dice {

//  def main(args: Array[String]): Unit = {
//    val dice = new Dice
//    dice.roll
//  }
//
//  val poss: Int = 36
//  final val expectations: Array[Double] = Array(0, 1/poss, 2/poss, 3/poss, 4/poss, 5/poss, 6/poss, 5/poss, 4/poss, 3/poss, 2/poss, 1/poss)

}
